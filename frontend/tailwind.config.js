/** @type {import('tailwindcss').Config} */
export default {
	// Tailwind CSS v4 is configured via the PostCSS plugin (@tailwindcss/postcss).
	// This file is optional; keep it for future theme/customization if needed.
	// Example:
	// theme: { extend: {} },
	// plugins: [],
};

